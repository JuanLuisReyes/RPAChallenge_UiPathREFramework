from reFramework.getMaestro import *

def raiseAlert(title, message, alert_Type):
    maestro.alert(
        task_id=execution.task_id,
        title=title,
        message=message,
        alert_type=alert_Type
    )

def infoAlert(title, message, alert_Type):
    maestro.alert(
        task_id=execution.task_id,
        title=title,
        message=message,
        alert_type=alert_Type
    )

def infoAlert(title, message, alert_Type):
    maestro.alert(
        task_id=execution.task_id,
        title=title,
        message=message,
        alert_type=alert_Type
    )
